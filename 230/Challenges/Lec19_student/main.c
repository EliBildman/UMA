#include "challenge.h"
#include <stdlib.h>

int main (int argc, char **argv) {
  // init(1);
  // reset_accs();
  // pthread_t *tids = (pthread_t *) calloc(sizeof(pthread_t), argc - 1);
  // struct args *as = (struct args *) calloc(sizeof(struct args), argc - 1);
  // for(int i = 0; i < argc - 1; i++) {
  //   as[i] = (struct args) {atoi(argv[i + 1]), 0};
  //   pthread_create(tids + i, NULL, count_up, as + i);
  // }
  // for(int i = 0; i < argc - 1; i++) {
  //   pthread_join(tids[i], NULL);
  // }
  // free(tids);
  // free(as);
  // clean();
  // printf("%i\n", get_acc_up());
  return 0;
}



